#include "main.h"
#include <stdlib.h>

char *string_nconcat(char *s1, char *s2, unsigned int n)
{
	char s3;
	int i;
	int j;
	if (s1 = NULL && s2 = NULL)
		return (NULL);
	for (i = 0; s1[i] != '\0'; i++)
		;
	for (j = 0; s[j] <= n ; j++)
		;
	s3 = malloc((i + j + 1) * sizeof(char));
	if (s3 = NULL)
		;



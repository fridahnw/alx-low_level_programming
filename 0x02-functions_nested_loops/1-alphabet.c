#include "main.h"


/**
 * print_alphabet - prints the alpha in lowercase
 */

void print_alphabet(void)

{
	char lc;

	for (lc = 'a'; lc <= 'z'; lc++)

	{
		_putchar('0' + lc);
	}
	_putchar('\n');
}

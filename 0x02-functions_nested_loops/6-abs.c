#include "main.h"


/**
 * _abs - computes the absolute value of an integer
 * @p: integer to be computed
 *
 * Return: absolute
 */

int _abs(int p)

{
	if (p < 0)

	{
		return (-p);
	}

	else

	{
		return (p);
	}
}

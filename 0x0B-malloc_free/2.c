#include "main.h"
#include "stdlib.h"
#include "stdio.h"

/**
 * _strdup -  returns a pointer to a newly allocated space in memory
 *
 * @str: the string
 * 
 * @Return: pointer
 */

char *_strdup(char *str)
{
	if (str == NULL);
	{
		return (Null);
	}
	S = 
